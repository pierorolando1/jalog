services:
  frontend:
    build: ./frontend
    ports:
      - "5173:5173"  # Format is "HOST:CONTAINER"
    environment:
      - NODE_ENV=development
  java-app:
    build: ./java-prolog-api
    ports:
      - "8080:8080"
    environment:
      - PROLOG_HOST=prolog
      - PROLOG_PORT=4000
